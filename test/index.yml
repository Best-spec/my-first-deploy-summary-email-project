info:
  name: index
  type: http
  seq: 1

http:
  method: GET
  url: http://127.0.0.1:8000/api/index/
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​