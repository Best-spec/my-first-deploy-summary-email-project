{
  "name": "summary-email-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "pip install -r requirements.txt && cd frontend-next && npm install",
    "build:backend": "pip install -r requirements.txt && python manage.py migrate --noinput",
    "build:frontend": "cd frontend-next && npm install && npm run build",
    "build": "./vercel-build.sh",
    "dev:backend": "python manage.py runserver 8000",
    "dev:frontend": "cd frontend-next && npm run dev",
    "start:backend": "gunicorn main.settings.wsgi:application --bind 0.0.0.0:8000",
    "start:frontend": "cd frontend-next && npm run start"
  }
}
